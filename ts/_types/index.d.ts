/* eslint-disable no-unused-vars */
interface Point {
    x: number;
    y: number;
}

interface Coordinate {
    x: number;
    y: number;
}

interface Color {
    red: number,
    green: number,
    blue: number
}
